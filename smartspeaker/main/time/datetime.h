#ifndef DATETIME_H
#define DATETIME_H

// Define a custom struct to hold time components
struct DateTime {
    int year;
    int month;
    int day;
    int hour;
    int minute;
    int second;
};

#endif // DATETIME_H